const express = require('express');
const router = express.Router();

// Import routers when ready, e.g.:
// const authRoutes = require('./auth');
// router.use('/auth', authRoutes);

module.exports = router;
